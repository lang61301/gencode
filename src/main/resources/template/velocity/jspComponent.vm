#macro( datatable_columns $jspColumns )
#set($j = 0)
	#foreach( $column in $jspColumns)
		#if($j > 0),#end
		#if( ${column.listRender.show} )
			${column.listRender.render}
			#set($j = $j + 1)
		#end
	#end
#end

#macro( ajax_pks_get_data $cols $sqlMapMarkUse )
var objData = {};
#foreach( $column in ${cols}) #if (${column.primary}) #if(${sqlMapMarkUse}==1) objData["${column.propertyName}"]=rowData["${column.propertyName}"]; #else objData["${column.columnName}"]=rowData["${column.columnName}"]; #end #end 
#end
#end
